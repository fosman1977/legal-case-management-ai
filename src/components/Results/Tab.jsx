/**
 * Tab Component - Week 6 Day 1-2
 * Individual tab component
 */

import React from 'react';

const Tab = ({ id, label, active, onClick, children }) => {
  // Tab is a logical component, actual rendering is handled by TabContainer
  return null;
};

export default Tab;